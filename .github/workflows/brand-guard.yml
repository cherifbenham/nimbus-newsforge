name: Brand Guard

on:
  push:
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run brand guard
        run: bash scripts/brand_guard.sh

